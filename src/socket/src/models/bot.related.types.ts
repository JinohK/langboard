export enum EBotPlatform {
    Default = "default",
    Langflow = "langflow",
}

export enum EBotPlatformRunningType {
    Default = "default",
    Endpoint = "endpoint",
    FlowJson = "flow_json",
}
